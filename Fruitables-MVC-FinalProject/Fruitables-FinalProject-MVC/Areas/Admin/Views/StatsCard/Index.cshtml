@model IEnumerable<StatsCard>
<div class="container">
    <h1 class="text-center mt-5">Stats Card</h1>
    <div class="table-responsive">
        <a class="btn btn-success" asp-action="Create">Create</a>
        <table class="table mt-2">
            <thead>
                <tr>
                    <th scope="col">Icon Name</th>
                    <th scope="col">Name</th>
                    <th scope="col">Value</th>
                    <th scope="col">Is Percent</th>
                    <th scope="col">Operations</th>
                </tr>   
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.IconName</td>
                        <td>@item.Name</td>
                        <td>@item.Value</td>
                        <td>@item.IsPercent</td>
                        <td class="d-flex" style="gap:10px;">
                            <button class="btn btn-danger delete-scard-btn" data-id="@item.Id">Delete</button>
                            <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                            <a class="btn btn-primary" asp-action="Detail" asp-route-id="@item.Id">Detail</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>